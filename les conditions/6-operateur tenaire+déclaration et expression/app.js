//let prix= contion ? vrai alors exp: faux alors exp


let prix = 100;
let choix = prix > 200 ? "premium" : "basique";

console.log(choix);


//déclaration et expression

let tutu = 500; // les expression son des valeur

if (prix > 500) {
    prix = prix + 100
} // c'est une déclaratiion